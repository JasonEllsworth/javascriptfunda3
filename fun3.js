//add 7, takes a number and adds 7!

function number(num) {
    let value = num + 7;
    return value;
}

// take two values, than multiply


// converts letters to upper case

function capital(word) {
   let capitalized = word[0].toUpperCase();
   return capitalized;
   
}